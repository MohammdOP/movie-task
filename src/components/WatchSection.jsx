import {
  SectionContainer,
  SectionTitle,
  SectionBadge,
} from "./WatchSection.styles";

function WatchSection() {
  return (
    <SectionContainer>
      <SectionTitle>
        Where To Watch <SectionBadge>29</SectionBadge>
      </SectionTitle>
    </SectionContainer>
  );
}

export default WatchSection;
